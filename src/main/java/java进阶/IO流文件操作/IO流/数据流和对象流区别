数据流和对象流都是 Java IO 中的一种流，它们的作用是存储和读取数据。两者的区别如下：

数据流 (Data Stream)
数据流是一种基本的二进制 IO 流，可以通过数据流将任意 Java 数据类型以内部格式写出到文件中，然后再从文件中读取相同的数据类型。
常用的数据流有 DataInputStream 和 DataOutputStream。可以使用数据流来实现基本数据类型和字符串的输入输出。

对象流 (Object Stream)
对象流是一种高级的 IO 流，可以将 Java 对象直接写入文件中，而不用担心对象中的数据成员的顺序。
也可以从文件中读取 Java 对象，而无需知道对象的具体类型。常用的对象流有 ObjectOutputStream 和 ObjectInputStream。
可以使用对象流来实现自定义类型的序列化和反序列化。

区别：

数据流和对象流的基本区别在于读写的数据类型不同。数据流主要用于读写基本的数据类型，而对象流主要用于读写 Java 对象。
数据流存储的是基础数据类型和字符串等数据，而对象流可以存储任意的 Java 对象，包括自定义类等。
数据流对于特定数据类型的读写具有内置性，仅需要调用其提供的方法即可，而对象流则需要实现 Serializable 接口来进行对象的读写。
因此，根据不同的需求，我们可以选择使用数据流或对象流。如果需要基于预定义数据类型进行低层次的输入输出，则应该使用数据流。
而如果需要在高层次上读写大量的对象，则应该使用对象流。